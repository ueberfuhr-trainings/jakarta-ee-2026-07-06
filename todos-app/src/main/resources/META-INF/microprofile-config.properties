# MicroProfile-Config-Quelle der Anwendung (Ordinal 100).
# Aktiviert das Anlegen der Beispieldaten beim Start (siehe SampleDataInitializer).
# Konvention im Code ist deaktiviert (defaultValue="false"); hier schalten wir
# es fuer die Anwendung ein. Ueberschreibbar durch hoeher priorisierte Quellen
# wie Umgebungsvariablen (TODOS_SAMPLEDATA_ENABLED) oder System-Properties.
todos.sampledata.enabled=true
